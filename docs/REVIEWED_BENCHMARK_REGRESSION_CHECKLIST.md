# Reviewed Benchmark Regression Checklist

Before wiring this gate into live CI:

- promote at least one explicitly approved record;
- generate a fresh bounded sample artifact;
- confirm provider title and year manually;
- confirm expected traits are represented by deterministic observed signals;
- run the evaluator and inspect every failure;
- keep unreviewed records non-gating.